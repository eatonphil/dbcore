database:
  dialect: postgres
  # dsn: root@/todo?sql_mode=ANSI
  dsn: postgres://todo:todo@/todo?sslmode=disable

address: localhost:9090

allowed-origins:
  - http://localhost:9091

session:
  secret: TODO
